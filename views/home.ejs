<% layout('layout') -%>
<h2>Welcome to Sir Larry Photography</h2>
<section class="gallery">
  <% photos.forEach(photo => { %>
    <div class="photo-card">
      <img src="<%= photo.imageUrl %>" alt="<%= photo.title %>">
      <p><%= photo.title %></p>
    </div>
  <% }) %>
</section>